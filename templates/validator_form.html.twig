{% extends 'base.html.twig' %}
{% block title %} Валидатор формата Базы вопросов{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <style>
        p.first {
            display: inline;
        }
        div.field {
            margin-top: 0.5em;
            margin-bottom: 0.9em;
        }
        .field p {
            margin-top: 3px;
            margin-bottom: 3px;
            text-indent: 2em;
        }
        .col-sm {
            max-width: 50%;
        }
        div.razdatka {
            background-color: #EEEEFF;
            padding: 4px;
            margin-top: 35px;
            border: 1px solid black;
        }
        div.razdatka:before {
            content: "Раздаточный материал";
            margin-top: -29px;
            margin-bottom: 9px;
            margin-left: -5px;
            padding-left: 4px;
            display: block;
            border: 1px solid black;
            background-color: #EEEEFF;
            width: 230px;
            height: 25px;
        }
        audio {
            width: 600px;
        }
    </style>
{% endblock %}
{% block body %}
    <div class="container-fluid">
        <h1>Валидатор формата <a href="https://db.chgk.info">Базы Вопросов</a></h1>

        <p>Введите текст в форму и нажмите "Проверить". Если ошибок нет, файл можно <a href="https://db.chgk.info/node/add/unsorted">добавлять</a> в Базу.
    <div class="row">
        <div class="col-sm">
            <form method="POST">
                <input type="submit" class="form-control" value="Проверить" />
                <textarea name="package_text" class="form-control" id="package_text" rows="1000">{{ plain_text }}</textarea>
            </form>
        </div>
        <div class="col-sm">
            {% if error %}
                <div class="alert alert-danger">{{ error }}</div>
                <pre>{{ result | raw }}</pre>
            {% else %}
                {{ result |raw }}
            {% endif %}
            <pre>
        </div>
    </div>
    </div>
{% endblock %}
